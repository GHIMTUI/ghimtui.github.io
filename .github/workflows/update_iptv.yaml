name: Extract IPTV Link to Text File With M3U8 Extension

on:
  schedule:
    - cron: '0 */2 * * *' # Tự động chạy mỗi 2 tiếng một lần
  workflow_dispatch: # Nút bấm chạy bằng tay bất cứ lúc nào

jobs:
  extract-link:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 20

    - name: Run JavaScript Extract Link
      run: |
        node -e '
        const http = require("http");
        const fs = require("fs");

        const targetUrl = "http://hoiquan.dpdns.org/VTVGo/?sctv4";

        function requestWithBypass(url, retryCount = 0) {
            if (retryCount > 3) {
                console.log("Quá số lần chuyển hướng cho phép.");
                return;
            }

            console.log("Đang kết nối tới: " + url);
            
            http.get(url, {
                headers: { 
                    "User-Agent": "Mozilla/5.0 (Linux; Android 10; K) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/119.0.0.0 Mobile Safari/537.36",
                    "Accept": "text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,image/apng,*/*;q=0.8",
                    "Accept-Language": "vi-VN,vi;q=0.9,en-US;q=0.8,en;q=0.7",
                    "Cache-Control": "max-age=0",
                    "Upgrade-Insecure-Requests": "1",
                    "Connection": "keep-alive"
                }
            }, (res) => {
                // Trường hợp 1: Server trả về mã chuyển hướng 301, 302, 307 (Trúng link thật)
                if ([301, 302, 307].includes(res.statusCode) && res.headers.location) {
                    let realUrl = res.headers.location.trim();
                    
                    // Nếu link trả về là link tương đối, tự động nối thêm domain vào
                    if (realUrl.startsWith("/")) {
                        realUrl = "http://hoiquan.dpdns.org" + realUrl;
                    }

                    // Nếu link đích cuối cùng chứa đuôi luồng phát hoặc token
                    if (realUrl.includes("m3u8") || realUrl.includes("vtvgo.vn") || retryCount === 2) {
                        // Ép đuôi .m3u8 chuẩn chỉnh
                        if (!realUrl.endsWith(".m3u8")) {
                            realUrl = realUrl.includes("?") ? realUrl + "&file=.m3u8" : realUrl + "?file=.m3u8";
                        }
                        fs.writeFileSync("link_sctv.txt", realUrl);
                        console.log("=== BÓC TÁCH THÀNH CÔNG ===");
                        console.log("Đã lưu vào link_sctv.txt: " + realUrl);
                    } else {
                        // Nếu vẫn là link trung gian tiếp theo, đệ quy chạy tiếp để bóc lớp vỏ thứ 2
                        requestWithBypass(realUrl, retryCount + 1);
                    }
                } 
                // Trường hợp 2: Server trả về mã 200 nhưng cố tình ẩn link trong nội dung HTML
                else if (res.statusCode === 200) {
                    let data = "";
                    res.on("data", (chunk) => { data += chunk; });
                    res.on("end", () => {
                        // Dùng Regex quét tìm mọi link .m3u8 ẩn trong mã nguồn HTML
                        const m3u8Regex = /(https?:\/\/[^\s"\']+\.m3u8[^\s"\']*)/i;
                        const match = data.match(m3u8Regex);
                        
                        if (match && match[1]) {
                            let realUrl = match[1].trim();
                            if (!realUrl.endsWith(".m3u8")) {
                                realUrl = realUrl.includes("?") ? realUrl + "&file=.m3u8" : realUrl + "?file=.m3u8";
                            }
                            fs.writeFileSync("link_sctv.txt", realUrl);
                            console.log("=== BÓC TÁCH THÀNH CÔNG TỪ HTML ===");
                            console.log("Đã tìm thấy link: " + realUrl);
                        } else {
                            console.log("=== THẤT BẠI ===");
                            console.log("Server trả về mã 200 nhưng không có link m3u8 nào ẩn bên trong.");
                        }
                    });
                } else {
                    console.log("=== BỊ CHẶN ===");
                    console.log("Mã lỗi: " + res.statusCode);
                }
            }).on("error", (err) => {
                console.error("Lỗi mạng: " + err.message);
            });
        }

        requestWithBypass(targetUrl);
        '

    - name: Commit and Push changes
      run: |
        git config --global user.name "GitHub Action Bot"
        git config --global user.email "actions@github.com"
        git add link_sctv.txt
        git commit -m "Bypass 403 và cập nhật link m3u8" || echo "Không có thay đổi"
        git push
